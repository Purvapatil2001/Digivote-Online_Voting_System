package com.app.services;
import com.app.entities.Executive;

public interface ExecutiveServices {

    // Register a new executive
    String registerExecutive(Executive executive);

    // Check login credentials
    Executive checkLogin(String ename, String password);
}
