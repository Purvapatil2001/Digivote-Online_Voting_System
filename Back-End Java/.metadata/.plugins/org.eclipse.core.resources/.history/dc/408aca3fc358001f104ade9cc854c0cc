package com.app.dao;

import org.springframework.data.jpa.repository.JpaRepository;
import com.app.entities.Executive;

public interface ExecutiveDAO extends JpaRepository<Executive, Long> {
    Executive findByEnameAndPassword(String ename, String password);

	Object findByEname(String ename);
}
